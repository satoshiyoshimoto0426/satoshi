dist
node_modules
coverage
pnpm-lock.yaml

# テスト用フィクスチャ(test/fixtures)は「バイト列そのもの」に意味があるため整形しない。
# 特に list-municipal-sjis.html は Shift_JIS で保存してあり、prettier が UTF-8 として
# 読み書きすると内容が壊れる(自治体サイトの文字コード対策テストが無意味になる)。
test/fixtures
